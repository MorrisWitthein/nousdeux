name: Build & Push API

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "api/**"
      - "Dockerfile.api"

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v6

      - name: Build and push
        run: |
          IMAGE=registry.tail32699d.ts.net/nousdeux-api
          TAG=v$(cat api/VERSION)
          docker build \
            -t $IMAGE:$TAG \
            -t $IMAGE:latest \
            -f Dockerfile.api ./api
          docker push $IMAGE:$TAG
          docker push $IMAGE:latest
