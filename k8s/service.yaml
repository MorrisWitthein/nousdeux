apiVersion: v1
kind: Service
metadata:
  name: nosdeux
  namespace: default
spec:
  selector:
    app: nosdeux
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080   # Access via http://<pi-tailscale-ip>:30080
