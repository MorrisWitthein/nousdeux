apiVersion: apps/v1
kind: Deployment
metadata:
  name: nousdeux-api
  namespace: default
  labels:
    app: nousdeux-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nousdeux-api
  template:
    metadata:
      labels:
        app: nousdeux-api
    spec:
      containers:
        - name: nousdeux-api
          image: registry.tail32699d.ts.net/nousdeux-api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: API_ADDR
              value: ":8080"
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 10
          resources:
            requests:
              memory: "16Mi"
              cpu: "25m"
            limits:
              memory: "32Mi"
              cpu: "100m"
