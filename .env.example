# Copy to .env.local (gitignored) for local development
VITE_API_URL=http://localhost:8080

# API env vars (set in your shell or .env when running `go run ./api`)
# JWT_SECRET=changeme
# USERS={"max":"<bcrypt-hash>","lena":"<bcrypt-hash>"}
